{
  "name": "carbonsense",
  "private": true,
  "version": "1.0.0",
  "scripts": {
    "dev": "concurrently \"npm run dev --prefix backend\" \"npm run start:watch --prefix frontend\"",
    "dev:frontend": "npm run start:watch --prefix frontend",
    "dev:backend": "npm run dev --prefix backend"
  },
  "devDependencies": {
    "concurrently": "^9.2.1"
  }
}
